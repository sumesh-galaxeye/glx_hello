from setuptools import setup, find_packages

setup(
    name='glx_hello',
    version='0.0.1',
    author='Sumesh_galaxeye',
    description='This is how we say hello at Galaxeye',
    packages=find_packages(),
    package_dir={'': 'src'},
)